#ifndef CONSTANTE_HPP
#define CONSTANTE_HPP

const int BLOCK_SIZE = 4096; // 4KB = 4096 bytes 
const int NUM_BUCKETS = 1; //Quantidade de buckets
const int NUM_BLOCKS = 1; // Quantidade de blocos por bucket
const int MAX_KEYS = 510; // Quantidade máxima de chaves por bloco na árvore B+

#endif